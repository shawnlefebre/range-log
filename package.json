{
  "name": "range-log",
  "version": "1.0.0",
  "description": "Dev tooling and regression tests for the Range Log PWA. The app itself (index.html, app.css, app.js, sw.js) has zero runtime dependencies and needs nothing from here to run.",
  "private": true,
  "scripts": {
    "test": "node --test test/app.test.js",
    "test:browser": "node test/browser/run.js"
  },
  "devDependencies": {
    "jsdom": "^24.0.0",
    "playwright": "^1.62.1"
  }
}
